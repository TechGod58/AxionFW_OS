{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "leader election integrity schema",
  "type": "object",
  "required": ["status", "leader_election", "failures"],
  "properties": {
    "status": {"type": "string", "enum": ["PASS", "FAIL"]},
    "leader_election": {"type": "object"},
    "failures": {"type": "array", "items": {"type": "object"}}
  }
}
