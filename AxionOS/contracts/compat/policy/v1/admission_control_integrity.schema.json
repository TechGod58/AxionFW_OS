{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "admission control integrity schema",
  "type": "object",
  "required": ["status", "admission_control", "failures"],
  "properties": {
    "status": {"type": "string", "enum": ["PASS", "FAIL"]},
    "admission_control": {"type": "object"},
    "failures": {"type": "array", "items": {"type": "object"}}
  }
}
