{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "secrets handling integrity schema",
  "type": "object",
  "required": ["status", "secrets", "failures"],
  "properties": {
    "status": {"type": "string", "enum": ["PASS", "FAIL"]},
    "secrets": {"type": "object"},
    "failures": {"type": "array", "items": {"type": "object"}}
  }
}
