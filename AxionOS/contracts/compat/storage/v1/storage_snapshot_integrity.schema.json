{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "storage snapshot integrity schema",
  "type": "object",
  "required": ["status", "storage_snapshot", "failures"],
  "properties": {
    "status": {"type": "string", "enum": ["PASS", "FAIL"]},
    "storage_snapshot": {"type": "object"},
    "failures": {"type": "array", "items": {"type": "object"}}
  }
}
