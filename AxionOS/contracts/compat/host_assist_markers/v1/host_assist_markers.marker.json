{
  "contract_id": "host_assist_markers",
  "version": "1.0",
  "required_markers": [
    {"name": "HOST_ASSIST_READY", "required_fields": ["run_id", "timestamp_utc"], "optional_fields": ["build_id"]}
  ]
}
