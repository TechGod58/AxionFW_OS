{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "scheduler decision determinism integrity schema",
  "type": "object",
  "required": ["status", "scheduler_determinism", "failures"],
  "properties": {
    "status": {"type": "string", "enum": ["PASS", "FAIL"]},
    "scheduler_determinism": {"type": "object"},
    "failures": {"type": "array", "items": {"type": "object"}}
  }
}
