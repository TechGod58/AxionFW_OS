{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "configuration immutable field policy schema",
  "type": "object",
  "required": ["status", "policy", "failures"],
  "properties": {
    "status": {"type": "string", "enum": ["PASS", "FAIL"]},
    "policy": {"type": "object"},
    "failures": {"type": "array", "items": {"type": "object"}}
  }
}
