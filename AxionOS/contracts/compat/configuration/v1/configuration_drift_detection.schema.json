{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "configuration drift detection schema",
  "type": "object",
  "required": ["status", "drift", "failures"],
  "properties": {
    "status": {"type": "string", "enum": ["PASS", "FAIL"]},
    "drift": {"type": "object"},
    "failures": {"type": "array", "items": {"type": "object"}}
  }
}
