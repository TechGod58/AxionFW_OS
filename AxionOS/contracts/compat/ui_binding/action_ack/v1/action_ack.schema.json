{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ui action acknowledgment schema",
  "type": "object",
  "required": ["status", "ack", "failures"],
  "properties": {
    "status": {"type": "string", "enum": ["PASS", "FAIL"]},
    "ack": {"type": "object"},
    "failures": {"type": "array", "items": {"type": "object"}}
  }
}
