{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "event bus delivery integrity schema",
  "type": "object",
  "required": ["status", "event_bus_delivery", "failures"],
  "properties": {
    "status": {"type": "string", "enum": ["PASS", "FAIL"]},
    "event_bus_delivery": {"type": "object"},
    "failures": {"type": "array", "items": {"type": "object"}}
  }
}
