{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "update rollout integrity schema",
  "type": "object",
  "required": ["status", "update_rollout", "failures"],
  "properties": {
    "status": {"type": "string", "enum": ["PASS", "FAIL"]},
    "update_rollout": {"type": "object"},
    "failures": {"type": "array", "items": {"type": "object"}}
  }
}
