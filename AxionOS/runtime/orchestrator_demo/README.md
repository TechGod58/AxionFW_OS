# Orchestrator E2E Demo

Runs one correlation-linked flow:
1. VM allocation decision (allocator)
2. Artifact generation (sandbox simulation)
3. Promotion decision (promotion daemon)

## Run

```powershell
python "$env:AXIONOS_ROOT\runtime\orchestrator_demo\run_e2e_demo.py"
```
