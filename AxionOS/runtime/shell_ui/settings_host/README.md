# Settings Host Runtime Scaffold

Implements Win11-style settings host state model:
- category navigation + breadcrumbs
- search query state
- preference updates with validation
- recent change log (corr-aware)

Next:
- deep-link route map
- bind control panel split-launch paths
- connect shell event bus + persistence backend
